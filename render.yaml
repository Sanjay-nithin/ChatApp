services:
  - type: web
    name: chatapp
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "chatapp-h5oc.onrender.com,*.onrender.com"
